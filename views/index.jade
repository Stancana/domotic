extends layout

block content
    h1= title
    p Welcome to #{title}
    form(name="send_mail", method="post", action="/")
        input(type="text", name="content")
        input(type="submit", value="add")
    if mail_ok
        p #{mail_ok}
    if mail_not_ok
        p #{mail_not_ok}

